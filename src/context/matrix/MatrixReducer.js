// import {
//     SEARCH_USERS,
//     GET_USER,
//     GET_REPOS,
//     SET_LOADING,
//     CLEAR_USERS,
// } from "../../types"

export default (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}